default: &default
  adapter: postgresql
  encoding: unicode
  max_connections: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>

development:
  <<: *default
  database: medibook_development

test:
  <<: *default
  database: medibook_test

production:
  primary: &primary_production
    <<: *default
    database: medibook_production
    username: medibook
    password: <%= ENV["MEDIBOOK_DATABASE_PASSWORD"] %>
  cache:
    <<: *primary_production
    database: medibook_production_cache
    migrations_paths: db/cache_migrate
  queue:
    <<: *primary_production
    database: medibook_production_queue
    migrations_paths: db/queue_migrate
  cable:
    <<: *primary_production
    database: medibook_production_cable
    migrations_paths: db/cable_migrate
